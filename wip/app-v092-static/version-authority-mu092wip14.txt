
// MeshUtilz v0.9.2-wip.14 — final version authority; defeats delayed legacy WIP stamps
(function(){
const PRODUCT='MeshUtilz',VERSION='0.9.2-wip.14';
function stamp(){document.title=`${PRODUCT} v${VERSION}`;document.querySelectorAll('.version').forEach(x=>x.textContent=`v${VERSION}`);const av=document.querySelector('.about-version');if(av)av.textContent=`${PRODUCT} · Version ${VERSION}`;const title=document.querySelector('.title');if(title)title.textContent=PRODUCT;const bars=[...document.querySelectorAll('.mlr-statusbar,.mlr-wip8-statusbar')];bars.slice(1).forEach(x=>x.remove());if(bars[0]){const s=bars[0].querySelectorAll('span');if(s.length)s[s.length-1].textContent=`v${VERSION}`}}
function settle(){stamp();requestAnimationFrame(stamp);setTimeout(stamp,40);setTimeout(stamp,180);setTimeout(stamp,500)}
try{fileInput?.addEventListener('change',settle,true)}catch{};
for(const id of['replaceMode','mlrMode','generateBtn','generate','buildBtn','mlrGenerate','mlrModeBtn','muzExportNomTube']){const e=document.getElementById(id);if(e)e.addEventListener(e.tagName==='SELECT'?'change':'click',settle,true)}
window.addEventListener('load',settle);settle();
})();
